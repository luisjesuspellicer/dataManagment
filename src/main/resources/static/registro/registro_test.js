'use strict';

describe('frontEndApp.registro module', function() {

  beforeEach(module('frontEndApp.registro'));

  describe('registro controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registroCtrl = $controller('registroCtrl');
      expect(registroCtrl).toBeDefined();
    }));

  });
});
