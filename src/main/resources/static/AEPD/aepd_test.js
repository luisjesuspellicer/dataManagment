'use strict';

describe('myApp.AEPD module', function() {

  beforeEach(module('myApp.view1'));

  describe('AEPD controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
